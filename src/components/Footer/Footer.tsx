import React from "react";
import s from './Footer.module.css';

const Footer = () => {
    return (
        <footer className={s.footer}>
            <small>it-kamasutra.com</small>
        </footer>
    );
};

export default Footer;